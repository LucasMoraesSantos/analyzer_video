{
  "name": "@analyzer/config",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "node -e \"console.log('No tests yet for @analyzer/config (stage 1).')\""
  },
  "dependencies": {
    "zod": "^3.24.2"
  }
}
