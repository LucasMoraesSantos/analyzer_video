[build]
command = "npm install --workspaces --include-workspace-root && npm run build --workspace @analyzer/web"

[build.environment]
NODE_VERSION = "20"

# O plugin oficial do Next.js no Netlify é injetado automaticamente pelo runtime.
# Evitamos pin manual em netlify.toml para não ficar defasado.
